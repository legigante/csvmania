{% extends 'layouts/admin.html.twig' %}

{% block title %}{{ 'task.add'|trans }}{% endblock %}

{% block content %}

    <ul>
        <li>controler csv</li>
    </ul>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

{% endblock %}
